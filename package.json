{
  "scripts": {
    "test": "cypress open",
    "test-reporter-chrome":"cypress run --browser edge --reporter cypress-multi-reporters --reporter-options configFile=config-report-chrome.json --spec",
    "test-reporter-firefox":"cypress run --browser edge --reporter cypress-multi-reporters --reporter-options configFile=config-report-firefox.json --spec",
    "test-reporter-edge":"cypress run --browser edge --reporter cypress-multi-reporters --reporter-options configFile=config-report-edge.json --spec"
  },
  "license": "MIT",
  "author": "ValentinoHuanca",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "cypress": "^13.13.1",
    "cypress-multi-reporters": "^1.6.4",
    "junit-report-merger": "^7.0.0",
    "mocha": "^10.7.0",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
